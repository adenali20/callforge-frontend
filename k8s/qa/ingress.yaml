apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: callforge-frontend
  namespace: qa
spec:
  ingressClassName: nginx
  rules:
    - host: qa.callforge.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: callforge-frontend
                port:
                  number: 80